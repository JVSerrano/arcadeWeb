import type { NextConfig } from "next";

// Configuración de Next.js del proyecto (sin opciones personalizadas por ahora)
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
